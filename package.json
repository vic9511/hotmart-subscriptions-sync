{
	"name": "hotmart-subscriptions-sync",
	"version": "1.0.0",
	"description": "**Supabase ↔ Hotmart integration** — syncs subscriptions, logs events, and verifies user access in your own database — **webhook-based**.",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"prepare": "husky",
		"format": "prettier --write ."
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"husky": "^9.1.7",
		"prettier": "^3.6.2"
	}
}
